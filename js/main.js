document.addEventListener('DOMContentLoaded',()=>{const products=[{name:'منتج تجريبي',description:'واجهة أولية قابلة للتوسع.'},{name:'منتج ثانٍ',description:'بيانات محلية مؤقتة إلى حين ربط API.'}];const list=document.querySelector('#product-list');if(list){list.replaceChildren(...products.map(p=>{const card=document.createElement('article');card.className='product-card';const title=document.createElement('h3');title.textContent=p.name;const desc=document.createElement('p');desc.textContent=p.description;card.append(title,desc);return card}))}const year=document.querySelector('#year');if(year)year.textContent=new Date().getFullYear();});